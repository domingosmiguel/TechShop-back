import { Router } from 'express';

// import controllers

// import middleware

const router = Router();

/// //////////////////////////
// router.use( 'middleware' )
/// //////////////////////////

// router.post('/sign-up', ...)
// router.post('/sign-in', ...)

export default router;
